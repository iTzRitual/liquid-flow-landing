/*
 * Barrel for the design-system tokens. Importing this module also loads the
 * token CSS (custom properties), so any consumer gets the variables in scope.
 */
import './tokens.css';

export * from './tokens.js';
