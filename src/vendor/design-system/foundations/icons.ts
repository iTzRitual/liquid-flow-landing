/**
 * The Lucide icon set used across the design system (the icons the Figma screens
 * reference), re-exported from one place so components import icons from the
 * design system, not directly from lucide-react.
 */
export {
  Zap,
  Shuffle,
  PackageSearch,
  Folder,
  FolderOpen,
  ChevronDown,
  Check,
  Lock,
  Plus,
  Lightbulb,
  Frown,
  Globe,
  RefreshCw,
  GitBranch,
  Download,
  Upload,
  Loader2,
  CircleDot,
  X,
} from 'lucide-react';

export type { LucideIcon } from 'lucide-react';
